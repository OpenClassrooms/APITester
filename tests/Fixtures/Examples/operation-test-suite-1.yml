#todo handle imports (to split into multiple files)

OperationTestCase1:
    #todo add description
    description: 'GHERKIN'

    for: [ 'findPetsByStatus' ]

    #todo handle data loaders
    dataLoad:
        pre: [ '%wonderland:senario1', 'group.alice' ]
        post: [ 'post.sql' ]

    #todo handle auth
    #    auth:
    #        type:
    #        token:
    #        uu

    #todo same format as open api examples
    request:
        parameters:
            query:
                id: '%fixtures.UserWithAllRoles.id%' #todo handle variables from fixtures
                status: 'available'
        headers:
            accept: application/json
    expectedResponse:
        statusCode: 200
    expectedData:
        - 'operation-1-checks.sql'
