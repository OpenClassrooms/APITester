#todo handle imports (to split into multiple files)

OperationTestCase1:
    #todo add description
    description: 'test test'

    for: [ 'findPetsByStatus' ]

    #todo handle data loaders
    dataLoad:
        pre: [ '!git:pre.sql', 'group.alice' ]
        post: [ 'post.sql' ]

    #todo handle auth
    #    auth:
    #        type:
    #        token:
    #        uu

    #todo same format as open api examples
    request:
        parameters:
            query:
                status: 'available'
        headers:
            accept: application/json
    expectedResponse:
        statusCode: 200
    expectedData:
        - 'operation-1-checks.sql'
